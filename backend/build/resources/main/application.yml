spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/GCCoffee?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 8354dnjsdl

# FOR H2 DATABASE
#    url: jdbc:h2:tcp://localhost/~/dev/java/Grids-Circles-Clone-APIServer
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:

  security:
    user:
      name: user
      password: 1234


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
    generate-ddl: true

  sql:
    init:
      platform: mysql
      encoding: UTF-8
      mode: always

