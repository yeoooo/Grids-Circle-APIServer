spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/dev/Grids-Circles-Clone-APIServer
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
#  sql:
#    init:
#      data-locations: sql/data.sql
#      platform: h2
#      encoding: UTF-8

